<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.roommate.roommateapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".mainscreen.AppMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chores.ChoresMainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".mainscreen.AppMainActivity" />
        </activity>

        <activity android:name=".chores.NewChoreActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".chores.ChoresMainActivity"/>
        </activity>
        <activity android:name=".chores.ShowChoreActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".chores.ChoresMainActivity"/>
        </activity>
        <activity android:name=".payments.PayMainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".mainscreen.AppMainActivity" />
        </activity>

        <activity android:name=".payments.NewPaymentActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".payments.PayMainActivity" />
        </activity>

        <activity android:name=".schedule.ScheduleMainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".mainscreen.AppMainActivity" />
        </activity>
        <activity android:name=".shoppinglist.ShoppingMainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".mainscreen.AppMainActivity" />
        </activity>
    </application>

</manifest>
